[package]
name = "ouroboros-dfg"
version = "0.1.0"
edition = "2021"
description = "Rust semantic verifier for Ouroboros v7.1 — DFG extraction and equality saturation"

[dependencies]
egg = "0.9.5"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
